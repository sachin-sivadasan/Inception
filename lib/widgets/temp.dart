import 'package:flutter/material.dart';

class temp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      child: Column(
        children: [
          Text('txtxtxt'),
          // Column(children: [Container(child: Text('hasjhflas'),)],)
        ],
      ),
    );
  }
}
